## NOTE: For local development you can set this to your backend (e.g. http://localhost:5001).
## For production deploys (Vercel), prefer setting VITE_API_URL as a project environment variable
## in the hosting platform. Leave empty in the repository to avoid baking localhost into builds.
VITE_API_URL=